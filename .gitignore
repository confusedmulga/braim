# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Claude Code local settings
.claude/

# Build artifacts anywhere
**/build/

# Kotlin compiler session files (build cache, not source)
.kotlin/
**/.kotlin/

# Release signing secrets — keep local, back them up privately.
android/key.properties
android/key.properties.ready
android/upload-keystore.jks

# Firebase / Google credentials — never commit (removed; app is local + Drive backup)
android/app/google-services.json
lib/firebase_options.dart
firebase.json
.firebaserc
ios/Runner/GoogleService-Info.plist
